import WorksheetGeneratorClient from "./WorksheetGeneratorClient";

export const metadata = {
  title: "Worksheet Generator - KiddoWorks",
  description: "Create custom printable math worksheets for kids in seconds.",
};

export default function WorksheetGeneratorPage() {
  return <WorksheetGeneratorClient />;
}
